import React from 'react';

export const ReviewList = props => (
    <div className="commentBlock">
      <h5 className="username">{props.username}</h5>
      <p className="comment">{props.username}</p>
    </div>
);

export default HotTopicComment;